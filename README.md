### 1번
3의 배수 + 5의 배수 - 15의 배수이므로,
$$\sum_{n=1}^{333} 3n + \sum_{n=1}^{199} 5n - \sum_{n=1}^{199} 15n $$

으로 계산 해 주었다.

---
### 2번
일단 피보나치 수열이 어떻게 생겼는지 쭉 나열해 보면,

1, 2, 3, 5, 8, 13, ...

과 같이 이어진다.

이때 각 항의 홀짝을 살펴보면,

(홀 / 짝 / 홀) / (홀 / 짝 / 홀) 

의 형태가 반복되는 것을 확인할 수 있다.

따라서 피보나치 수열에서 짝수인 항은 항상 항 번호가 $3$으로 나누었을 때 나머지가 $2$인 항이다.

즉, 우리가 구해야 하는 짝수 피보나치 항은

$F_2, F_5, F_8, F_{11}, ...$

과 같이 나타낼 수 있다.

따라서 항 번호가 $3k+2$인 일반항 $F_{3k+2}$를 이용하여 짝수 피보나치 항들의 규칙을 찾아보려고 한다.

<규칙 찾기>

$F_{3k+1} = F_{3k} + F_{3k-1}$

$F_{3k+2} = F_{3k+1} + F_{3k}$

$F_{3k+3} = F_{3k+2} + F_{3k+1}$

로 설정을 해 주었고,

$F_{3k+4} = F_{3k+3} + F_{3k+2}$

$F_{3k+5} = F_{3k+4} + F_{3k+3}$

이고, 우리가 $F_{3k+5}$에 대해서 정의를 해 주어야 한다고 생각을 하였다.

먼저, $$F_{3k+5} = F_{3k+4} + F_{3k+3} $$

이고, $F_{3k+4} = F_{3k+3} + F_{3k+2}$이므로

$$F_{3k+5} = 2F_{3k+3} + F_{3k+2} $$

이다. 또한 $F_{3k+3} = F_{3k+2} + F_{3k+1}$이므로

$$ F_{3k+5}= 2(F_{3k+2} + F_{3k+1}) + F_{3k+2} $$

$$ = 3F_{3k+2} + 2F_{3k+1} $$


---

### 3번
어떤 자연수 $n$이 $i$로 나누어떨어진다는 것은

$$
n \div i
$$

의 나머지가 $0$이라는 의미이다.

즉,

$$
n \bmod i = 0
$$

이면 $i$는 $n$의 약수이다.

이를 식으로 나타내면 어떤 자연수 $k$에 대하여

$$
n = ik
$$

로 나타낼 수 있다.

따라서 $i$가 $n$의 약수인지 확인하기 위해서는

$$
n \bmod i = 0
$$

인지 확인하면 된다.

또한 $n=ik$일 때 두 약수 $i$와 $k$의 곱이 $n$이므로, 두 수 중 하나는 반드시 $\sqrt n$ 이하이다.

따라서 $n$의 약수를 찾을 때는

$$
i^2 \le n
$$

즉,

$$
i \le \sqrt n
$$

인 범위까지만 확인하면 된다.

---

### 6번

보자마자 백준의 팩토리얼 0의 개수가 생각이 났다.

처음에는 $10 = 2 \times 5$이므로 $100!$에서 2와 5의 개수를 구하면 되지 않을까 생각했다. 하지만 이 방법으로는 $100!$의 끝에 존재하는 0의 개수만 구할 수 있고, 자릿수의 합을 구할 수는 없었다.

결국

$$
100! = 1 \times 2 \times 3 \times \cdots \times 100
$$

을 직접 계산해야 했다.

$100!$은 매우 큰 수이기 때문에 파이썬이 아닌 이상 C++로 에서의 정수로는 저장할 수 없다. 따라서 큰 수의 곱셈은 받아올림을 이용하여 구할 수 있는 알고리즘이 있고, 이걸 토대로 계산을 하였다.

배열 $a$의 각 원소를 $100!$의 각 자리라고 생각하였다.

예를 들어 현재 자리의 값이 $4$이고 $5$를 곱한다면

$$
4 \times 5 = 20
$$

이 된다.

이때 현재 자리에 들어갈 값은

$$
20 \bmod 10 = 0
$$

이고, 다음 자리로 올려줄 값은

$$
\left\lfloor \frac{20}{10} \right\rfloor = 2
$$

가 된다.

즉, 일반적인 곱셈에서 일의 자리를 남기고 십의 자리로 받아올림하는 과정을 코드로 구현한 것이다.